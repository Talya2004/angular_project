import { MeetingDatePipe } from './meeting-date.pipe';

describe('MeetingDatePipe', () => {
  it('create an instance', () => {
    const pipe = new MeetingDatePipe();
    expect(pipe).toBeTruthy();
  });
});
